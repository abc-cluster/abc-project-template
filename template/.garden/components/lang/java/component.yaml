id: "lang:java"
description: "Java (with Maven/Gradle) package development"
version: "0.1.0"
files:
  - jreleaser.yml
  - tasks/java.just
adds_imports:
  - tasks/java.just
post_grow_hint: |
  java recipes added under tasks/java.just.
  Note: experimental — recipe bodies are stubs.
