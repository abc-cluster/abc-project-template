id: "infra:packer"
description: "Packer image build configurations"
status: "experimental"
version: "0.1.0"
files:
  - infra/packer/image.json
post_grow_hint: |
  Created infra/packer/ with subsystem-specific scaffolding.
  Inspect the generated files; some require external tools (e.g. terraform, packer).
